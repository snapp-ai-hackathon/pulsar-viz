apiVersion: projectcontour.io/v1
kind: HTTPProxy
metadata:
  name: {{ include "pulsar-viz.fullname" . }}
  labels:
    {{- include "pulsar-viz.labels" . | nindent 4 }}
spec:
  ingressClassName: private
  routes:
    - services:
        - name: {{ include "pulsar-viz.fullname" . }}
          port: {{ .Values.service.port }}
          weight: 100
      timeoutPolicy:
        response: 2m
        idle: 2m
        idleConnection: 2m
  virtualhost:
    fqdn: "pulsar-viz.apps.private.okd4.teh-2.snappcloud.io"
