apiVersion: v1
kind: Service
metadata:
  name: {{ include "pulsar-viz.fullname" . }}
  labels:
    {{- include "pulsar-viz.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pulsar-viz.selectorLabels" . | nindent 4 }}
